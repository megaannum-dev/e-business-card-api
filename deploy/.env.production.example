# Copy to the repo root on the server as .env
#   cp deploy/.env.production.example .env

DEPLOY_ENV=prod

# Browser origins allowed cross-origin. Empty = no cross-origin browser access
# (correct if only the mobile app and the same-origin share page call the API).
CORS_ALLOWED_ORIGINS=

MONGO_ROOT_USERNAME=ebc_admin
MONGO_ROOT_PASSWORD=change_me_use_a_long_random_password
MONGO_DB_NAME=e_business_card
MONGO_CARDS_COLLECTION=captured_cards

OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_MODEL=deepseek/deepseek-chat
OPENROUTER_TIMEOUT_SECONDS=30
OPENROUTER_MAX_RETRIES=2

# US OpenRouter account — used only for scan image enhancement (openai/* models)
OPENROUTER_API_KEY_US=sk-or-v1-your-us-key-here
OPENROUTER_IMAGE_MODEL=google/gemini-3.1-flash-image
OPENROUTER_IMAGE_PROVIDER=google-vertex/global
OPENROUTER_IMAGE_TIMEOUT_SECONDS=30
OPENROUTER_IMAGE_QUALITY=high
OPENROUTER_IMAGE_ENHANCEMENT_ENABLED=true
OPENROUTER_IMAGE_MAX_ATTEMPTS=3

# Relative FIREBASE_CREDENTIALS_PATH values are resolved to an absolute repo-root path in deploy/start.sh
FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json

SHARE_PUBLIC_BASE_URL=https://ebc.megaannum.ai/c

# DEPLOY_ENV=prod hides Swagger UI (/docs, /redoc, /openapi.json).
# Do not change DEPLOY_ENV to dev here — that flag still selects this compose file.
# Mobile app connectivity: GET /health (always on).

# Scheduled MongoDB backups (deploy/backup_mongo.sh via crontab, daily 03:00).
BACKUP_ENABLED=true
BACKUP_HOUR=3
BACKUP_DIR=/opt/e-business-card-api/backups

# Auth / rate-limit alerting (deploy/alert_auth_anomalies.sh via crontab, every 5 min).
# Set the thresholds from your own baseline -- a steady trickle of 401s is normal
# (the app refreshes tokens on resume). See deploy/RUNBOOK.md.
ALERT_ENABLED=false
ALERT_WEBHOOK_URL=
ALERT_ACCESS_LOG=/var/log/nginx/ebc-access.log
ALERT_AUTHFAIL_THRESHOLD=50
ALERT_PER_IP_THRESHOLD=20
ALERT_THROTTLE_THRESHOLD=100
ALERT_COOLDOWN_SECONDS=1800
