# Dev / CMS co-hosted stack (focms.megaannum.ai:8001 → 127.0.0.1:8002).
# Standard Docker bridge networking — DNS works on the dev Alibaba VPS.
# Paths are relative to repo root — use: bash deploy/start.sh (DEPLOY_ENV=dev in .env)

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    container_name: ebc-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:8002:8000"
    environment:
      DEPLOY_ENV: ${DEPLOY_ENV:-dev}
      MONGO_URI: mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@mongodb:27017
      MONGO_DB_NAME: ${MONGO_DB_NAME:-e_business_card}
      MONGO_CARDS_COLLECTION: ${MONGO_CARDS_COLLECTION:-captured_cards}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      OPENROUTER_API_KEY_US: ${OPENROUTER_API_KEY_US:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-deepseek/deepseek-chat}
      OPENROUTER_TIMEOUT_SECONDS: ${OPENROUTER_TIMEOUT_SECONDS:-30}
      OPENROUTER_MAX_RETRIES: ${OPENROUTER_MAX_RETRIES:-2}
      OPENROUTER_IMAGE_MODEL: ${OPENROUTER_IMAGE_MODEL:-google/gemini-3.1-flash-image}
      OPENROUTER_IMAGE_PROVIDER: ${OPENROUTER_IMAGE_PROVIDER:-google-vertex/global}
      OPENROUTER_IMAGE_TIMEOUT_SECONDS: ${OPENROUTER_IMAGE_TIMEOUT_SECONDS:-30}
      OPENROUTER_IMAGE_QUALITY: ${OPENROUTER_IMAGE_QUALITY:-high}
      OPENROUTER_IMAGE_ENHANCEMENT_ENABLED: ${OPENROUTER_IMAGE_ENHANCEMENT_ENABLED:-true}
      OPENROUTER_IMAGE_MAX_ATTEMPTS: ${OPENROUTER_IMAGE_MAX_ATTEMPTS:-3}
      FIREBASE_CREDENTIALS_PATH: /app/secrets/firebase-service-account.json
      SHARE_PUBLIC_BASE_URL: ${SHARE_PUBLIC_BASE_URL:-https://focms.megaannum.ai:8001/c}
    volumes:
      - ${SECRETS_BIND_MOUNT}:/app/secrets:ro
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - ebc-internal

  mongodb:
    image: mongo:7
    container_name: ebc-mongodb
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - ebc-internal

networks:
  ebc-internal:
    driver: bridge

volumes:
  mongodb_data:
